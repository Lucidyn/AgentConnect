services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=true
      - DISTRIBUTED_WORKERS=true
      - WORKER_AGENTS=Research,Coder,Writer,Analyst,Translator,Reviewer,TestRunner
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - API_KEY=${API_KEY:-}
      - LLM_TIMEOUT_SECONDS=${LLM_TIMEOUT_SECONDS:-120}
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app/backend
      - ./plugins:/app/plugins
      - app_data:/app/data
    command: python -m backend.run
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  worker-research:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=true
      - WORKER_MODE=true
      - WORKER_AGENT_NAME=Research
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - app_data:/app/data
    command: python -m backend.worker.run

  worker-coder:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=true
      - WORKER_MODE=true
      - WORKER_AGENT_NAME=Coder
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - app_data:/app/data
    command: python -m backend.worker.run

  worker-reviewer:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=true
      - WORKER_MODE=true
      - WORKER_AGENT_NAME=Reviewer
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - app_data:/app/data
    command: python -m backend.worker.run

volumes:
  redis_data:
  app_data:
