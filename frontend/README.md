# Agent Connect UI (Vite)

Modular frontend sources for the web console.

```bash
cd frontend
npm install --cache ./.npm-cache
npm run build   # outputs to backend/static/assets/
```

Built assets are committed so the Python server works without Node. Rebuild after editing `src/*.js`.
