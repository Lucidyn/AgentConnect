[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    redis: integration tests requiring a running Redis instance
    postgres: integration tests requiring PostgreSQL (DATABASE_URL)
